// https://github.com/bbvch13531/SSU-cse-algorithm/blob/master/quickSort/main.cpp
public func QuickSort<T: Comparable>(_ array: [T], lo: Int, hi: Int) -> [T] {

}

func partition<T: Comparable>(_ array: [T], lo: Int, hi: Int) -> Int {
    var arr = array
    var pivot = array[hi]
    var pivotIndex = hi
    var tmp

    i in lo..<hi {
        if(arr[i] < pivot) {
            pi
        }
    }
}